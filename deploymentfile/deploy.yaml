
---
apiVersion: v1
kind: Service
metadata:
  name: insta-service
  labels:
    app: insta-app
    env: prod
spec:
  type: LoadBalancer  
  selector:
     app: insta-app
     env: prod
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
